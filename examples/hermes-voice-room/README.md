# Hermes Voice Room Example

This example shows a sanitized voice event that Agentarium could render in Command Centre and a private world.

See [`session-event.example.json`](session-event.example.json).

