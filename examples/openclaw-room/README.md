# OpenClaw Room Example

This example shows the public shape of an OpenClaw runtime manifest.

The goal is for OpenClaw to tell Agentarium:

- which room it belongs to
- which agents are present
- what capabilities are available
- what state the runtime is in
- where handoffs and escalations are happening

See [`runtime-manifest.example.json`](runtime-manifest.example.json).

